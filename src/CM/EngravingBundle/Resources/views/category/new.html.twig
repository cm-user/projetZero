{% extends 'base.html.twig' %}

{% block body %}

    <h1>Création d'une nouvelle catégorie</h1>


    <div class="row">
        <div class="col-sm-7">
            <br><br>
            {{ form_start(form) }}
            {#{{ form_widget(form) }}#}
            <label>Nom de la catégorie :<br> {{ form_widget(form.surname, {'attr': {'placeholder': '' }} ) }}
            </label>
            <br>
            <label>Nom du dossier dans le zip :<br> {{ form_widget(form.folder, {'attr': {'placeholder': '' }} ) }}
            </label>
            <br>
            <label> Id du produit :<br>{{ form_widget(form.idProduct, {'attr': {'value': idProduct} } ) }}
            </label>
            <br>
            <label>Temps de préparation en minutes :<br> {{ form_widget(form.time, {'attr': {'placeholder': '' }} ) }}
            </label>
            <br>
            <input class="btn btn-success" type="submit" value="Créer"/>
            {{ form_end(form) }}


            {#{{ form_start(form) }}#}
            {# render the task's only field: description #}
            {#{{ form_row(form.surname) }}#}
            {#{{ form_row(form.time) }}#}
            {#{{ form_row(form.IdProducts) }}#}
            {#{{ form_widget(form) }}#}

            {#<h3>Tags</h3>#}
            {#<ul class="tags">#}
                {# iterate over each existing tag and render its only field: name #}
                {#{% for tag in form.idProducts %}#}
                    {#<li>{{ form_row(tag.IdProduct) }}</li>#}
                {#{% endfor %}#}
            {#</ul>#}
            {#{{ form_end(form) }}#}

        </div>
        <div class="col-sm-5">
            <h3>Liste des Catégories</h3>

            <table class="table table-striped table-bordered">
                <thead>
                <tr>
                    <th>Id</th>
                    <th>Id du Produit</th>
                    <th>Nom Catégorie</th>
                    <th>Dossier</th>
                    <th>Temps(min)</th>
                    <th>Actions</th>
                </tr>
                </thead>
                <tbody>
                {% for category in categories %}
                    <tr>
                        <td><a href="{{ path('category_show', { 'id': category.id }) }}">{{ category.id }}</a></td>
                        <td>{{ category.idProduct }}</td>
                        <td>{{ category.surname }}</td>
                        <td>{{ category.folder }}</td>
                        <td>{{ category.time }}</td>
                        <td>

                            <a class="btn btn-info" href="{{ path('category_show', { 'id': category.id }) }}">show</a>

                            <a class="btn btn-warning"
                               href="{{ path('category_edit', { 'id': category.id }) }}">edit</a>

                        </td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
    <br>

    <a class="btn btn-info" href="{{ path('category_index') }}">Retour à la liste</a>



{% endblock %}
