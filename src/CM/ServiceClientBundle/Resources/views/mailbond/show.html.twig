{% extends 'base.html.twig' %}

{% block body %}
    <h1>Mailbond</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ mailBond.id }}</td>
            </tr>
            <tr>
                <th>Mail</th>
                <td>{{ mailBond.mail }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('mailbond_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('mailbond_edit', { 'id': mailBond.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
